Create a 16:9 presentation slide titled "Deployment" in the same Boxy slide style. Show a Docker container diagram with mounted volumes for uploads (/app/uploads) and app data (/app/data) and env vars BOX_PORT, BOX_UPLOAD_DIR, BOX_DATA_DIR, BOX_MAX_UPLOAD_BYTES. Include a readable command snippet: docker run -p 8086:8086 -v $(pwd)/uploads:/app/uploads -v $(pwd)/data:/app/data -e BOX_DATA_DIR=/app/data boxy. No Docker logos; use generic container icon.
