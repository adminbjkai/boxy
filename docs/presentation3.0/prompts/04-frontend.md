Create a 16:9 presentation slide titled "Frontend UX" in the same Boxy slide style. Center a clean UI mock (generic, no copied UI) showing top tabs (Home, Files, Tasks, Keys, About), a sticky toolbar, and a file list with clear headers and row hover. Add callouts: "Grid/List toggle", "Inline rename (list view)", "Bulk actions appear on selection", "Upload progress + per-file status", "Keyboard shortcuts". Keep text large and readable.
