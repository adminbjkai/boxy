Create a 16:9 presentation slide titled "Security Model" in the same Boxy slide style. Visualize defense-in-depth layers with three stacked bands: Path safety (resolve_path_safe, clean_relative_path), Resource limits (payload limit, search cap), Output escaping (escapeHtml/escapeAttr). Add red attack arrows blocked at each layer (../ traversal, symlink escape, huge upload, search DoS, XSS filename). Keep labels short and legible.
