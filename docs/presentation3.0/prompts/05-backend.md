Create a 16:9 presentation slide titled "Backend (Rust/Actix)" in the same Boxy slide style. Show a simple server box with grouped endpoint categories: Files (list/upload/download/content/newfile), Folder ops (folder/rename/move/delete), Search, Health, Data API (/api/data/{type}). Add a security callout card: "resolve_path_safe blocks traversal + symlink escapes" and a limits card: "BOX_MAX_UPLOAD_BYTES (200MB), MAX_SEARCH_RESULTS (100)". Use flat vector icons, no logos.
